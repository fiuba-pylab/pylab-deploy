monto_compra = int(input("ingrese el monto de la compra"))
if monto_compra < 50: 
    descuento = 0
else: 
    if monto_compra < 100: 
        descuento = 0.10
    else: 
        if monto_compra < 200: 
            descuento = 0.20
        else: 
            descuento = 0.30
monto_final = monto_compra * (1 - descuento); 
print(f"Tu compra fue de: {monto_final}")