n = input('Ingrese un número decimal: ')
binario = ""
if n == 0:
    print("0")
while n > 0:
    binario = str(n % 2) + binario
    if n % 2:
        n -= 1
    n /= 2
print(f'El número en binario es: {binario}')
