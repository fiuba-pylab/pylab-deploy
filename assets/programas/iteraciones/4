import math

num = int(input('Ingrese un número: '))
print("Sucesión malabatista de {num}: ", end=' ')
while num > 1:
    if num % 2 == 0: 
        num = int(math.sqrt(num))
    else: 
        num = int(math.sqrt(num**3)) 
    print(f'{num}')
